import { PrismaClient } from "@prisma/client";

const prisma = new PrismaClient();

export default function PrismaTemplate() {
	const createTemplate = async () => {
		try {
			
		} catch (error) {
			
		}
	}
  return <div>TemplateCreate</div>;
}